#include <ReaderWriter.hpp>

cv::Mat ReaderWriter::read(std::string imagePath) {
  return cv::Mat();
}

cv::Mat ReaderWriter::drawRectangle(cv::Mat image, cv::Rect boundary) {
  return cv::Mat();
}

void ReaderWriter::showImage(cv::Mat) {

}
